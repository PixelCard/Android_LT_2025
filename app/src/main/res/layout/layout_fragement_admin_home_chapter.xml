<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    card_view:cardCornerRadius="12dp"
    card_view:cardElevation="6dp">

    <LinearLayout
        android:id="@+id/itemContainerChapter_AdminForHome"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="10dp">

        <!-- Hình ảnh chương -->
        <ImageView
            android:id="@+id/imgProductForChapterHome"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:layout_marginEnd="10dp"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_launcher_background" />

        <!-- Thông tin chương -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <!-- Tên truyện -->
            <TextView
                android:id="@+id/tvTenTruyenForChapterHome"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Tên truyện"
                android:textColor="#000000"
                android:textSize="16sp"
                android:textStyle="bold" />

            <!-- Số chương -->
            <TextView
                android:id="@+id/tvChapterNumberHome"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Chương 1"
                android:textColor="#000000"
                android:textSize="14sp"
                android:layout_marginTop="2dp" />

            <!-- Nội dung chương -->
            <TextView
                android:id="@+id/tvChapterContentHome"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Đây là nội dung chương truyện..."
                android:textSize="14sp"
                android:textColor="#333333"
                android:layout_marginTop="4dp"
                android:ellipsize="end"
                android:maxLines="3" />
        </LinearLayout>
    </LinearLayout>
</androidx.cardview.widget.CardView>

